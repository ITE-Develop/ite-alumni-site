/*
import {defineStore} from 'pinia';
import {computed, ref} from 'vue';
import DenseService from "@/services/DenseService.js";

export const useDenseStore = defineStore('Dense', () => {

  const all = async () => {
    return DenseService.index({
      'city_id': 'Takeshiba'
    });
  }

  return {
    all
  }
});
*/
