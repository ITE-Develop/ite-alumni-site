/*
import {defineStore} from 'pinia';
import TrainService from '@/services/TrainService';

// export action name
export const useTrainStore = defineStore('train', () => {

  const all = async () => {
    return TrainService.index();
  }

  return {
    all
  }
});
*/
